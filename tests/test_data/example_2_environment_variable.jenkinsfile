pipeline {
    agent any
    environment {
        BUILD_MODE = 'release'
    }
    stages {
        stage('Build') {
            steps {
                sh 'make build'
            }
        }
    }
}